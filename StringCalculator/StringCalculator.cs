using System;
using System.Linq;

namespace StringCalculator
{
    public class StringCalculator
    {
        internal int Add(string v)
        {
            v>ingSplitOptions()
            if (v == "") return 0;
            return int.Parse(v);
        }
    }
}
